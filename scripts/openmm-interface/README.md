# Using ResFF in OpenMM
This directory provides the OpenMM interface for the ResFF.
To ensure compatible dependencies, install the environment via:

``` bash
mamba env create -f environment.yml
```

To start a simulation for the Tyk2 protein-ligand system, run:

``` bash
python run_simulation.py
```